using UnityEngine;

public enum ItemType
{
    None = 0,

    // Food / fruits
    Apple = 10,
    Pear = 11,
    Banana = 12,

    // Currency / rewards
    Coin = 20,
    Gem = 21,

    // Props / misc
    Box = 30,
    Rock = 31,
    Barrel = 32,
}